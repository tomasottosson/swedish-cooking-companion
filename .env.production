# Production environment configuration

# Proxy runs as an SWA Managed Function on the same origin as the frontend.
# Empty string => frontend calls the relative path /api/fetch-recipe.
VITE_PROXY_URL=
